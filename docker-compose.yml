services:
  qpos-db:
    image: postgres:16-alpine
    container_name: qpos-db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: qpos
      POSTGRES_USER: qpos
      POSTGRES_PASSWORD: qpos_secret
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U qpos -d qpos"]
      interval: 5s
      timeout: 5s
      retries: 5

  qpos-server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: qpos-server
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - JWT_SECRET=qpos-secret-change-in-production
      - DATABASE_URL=postgres://qpos:qpos_secret@qpos-db:5432/qpos
      - MASTER_DATABASE_URL=postgres://qpos:qpos_secret@qpos-db:5432/qpos_master
      - NODE_ENV=production
      - CORS_ORIGIN=https://www.qpos.shop,http://localhost:5173
      - APP_URL=https://www.qpos.shop
    depends_on:
      qpos-db:
        condition: service_healthy
    restart: unless-stopped

volumes:
  pgdata:
